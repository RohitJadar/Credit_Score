name: Python application

on:
  push:
   branches: [ master ]
  pull_request:
    branches: [ master ]
      
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build & Push Image
      run: |
        docker login -u "abhishekkaddipudi" -p dckr_pat_lSNw6_uuStkmLXFS0o0hNHtard8
        docker image build -t abhishekkaddipudi/credit_score:latest .
        docker push abhishekkaddipudi/credit_score:latest
