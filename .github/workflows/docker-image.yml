name: CI
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2


      - name: Scripts
        run: |
          docker build -t app .
          docker login abhishekkaddipudi -p dckr_pat_lSNw6_uuStkmLXFS0o0hNHtard8
          docker tag app abhishekkaddipudi/credit_score
          docker push abhishekkaddipudi/credit_score
